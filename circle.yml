machine:
  node:
    version: 0.10.36
# dependencies:
#   post:
#     - npm install codeclimate-test-reporter -g
test:
  override:
    - grunt coverage
#   post:
#     - codeclimate-test-reporter < test/coverage/lcov.info
deployment:
  release:
    tag: /^[0-9]+(\.[0-9]+)*/
    commands:
      - echo "deploying ..."
      # - grunt shipit:stg deploy
      # - NODE_ENV=production grunt shipit:databaseStg migrate-database
general:
  artifacts:
    - "test/coverage"